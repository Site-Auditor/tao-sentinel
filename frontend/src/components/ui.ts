/** Shared presentational constants used across dashboard sections. */

/** Section heading style: small, uppercase, faint, wide tracking. */
export const SECTION_HEADING =
  "text-[11px] uppercase tracking-[0.12em] text-ink-faint font-semibold mb-2";

/** Small netuid chip. */
export const NETUID_CHIP =
  "inline-flex items-center rounded-[4px] bg-surface-3 px-1.5 py-0.5 text-[11px] tnum text-ink-dim";
